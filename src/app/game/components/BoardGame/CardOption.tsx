import { X } from "lucide-react";
import React from "react";

const CardOption = () => {
  return (
    <div className="h-24 bg-secondaryGame rounded-xl flex items-center justify-center">
      <X size={80} className="text-red-500" />
    </div>
  );
};

export default CardOption;
